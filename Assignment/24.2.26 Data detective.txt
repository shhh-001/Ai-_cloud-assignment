import pandas as pd

# Load dataset (example: CSV file)
df = pd.read_csv("/content/students.csv")

# Display top 5 rows
print("Top 5 Rows:")
print(df.head())

# Find column with highest average value (numeric columns only)
highest_column = df.mean(numeric_only=True).idxmax()
print("Column with Highest Average Value:", highest_column)

# Count missing values
print("Missing Values in Each Column:")
print(df.isnull().sum())


